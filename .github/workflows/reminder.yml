- name: Send Discord message
        run: |
          cat > payload.json <<'EOF'
          {"content": "Don't forget to grab this week's redeem code: https://x.com/service"}
          EOF
          curl -H "Content-Type: application/json" -d @payload.json ${{ secrets.DISCORD_WEBHOOK_URL }}
